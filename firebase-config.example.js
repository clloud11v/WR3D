// Copy this file to `firebase-config.js` and fill with your Firebase project values.

window.FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  // messagingSenderId, appId, measurementId etc if provided
};

// Optional: list of admin emails to treat as admin when using Firebase Auth
window.FIREBASE_ADMIN_EMAILS = [
  'admin@example.com'
];
